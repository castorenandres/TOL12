program test;
variables 
    x, i : int;
    y, resultado : float;

void function escribir ();
{
    arc(50, 180);
    write("Hola mundo!");
}

float function sumar (int x, float y);
{
    escribir()
    return (x + y);
}

principal() {
    x = 20;
    y = 5;

    resultado = (sumar(x, y) + 5) * 2;
    write(resultado);
}